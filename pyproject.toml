[project]
name = "ruletale"
version = "0.1.0"
description = "Rule-horror game delivered through agentsystem."
requires-python = ">=3.13"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["apps/api/tests"]
